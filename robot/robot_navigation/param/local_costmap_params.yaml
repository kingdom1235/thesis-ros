local_costmap:
  global_frame: odom   #sample: odom (can use map)
  robot_base_frame: base_footprint

  update_frequency: 10.0
  publish_frequency: 10.0
  transform_tolerance: 5  

  static_map: true    #use with user-generated static map, if false use rolling_window
  rolling_window: true  #true if static map is false
  width: 2
  height: 2
  resolution: 0.05
